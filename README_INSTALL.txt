```
# create virtual env:
$ python3 -m venv .venv

# activate virtual env
$ source .venv/bin/activate
$ pip install -r requirements.txt
$ pip install -r test_requirements.txt

# deactivate virtual venv
$ deactivate

# to see installed list of packages
$ pip list

needed tkinter for matplotlib
# sudo apt-get install python3-tk
```


sudo apt-get install libpq-dev python-dev  to instal psycop
python-libpq-dev>=9.0.0
